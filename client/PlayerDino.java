import java.util.HashMap;

public class PlayerDino extends Player {
    public PlayerDino(String account, Controller controller, HashMap<String, Integer> scoreboard, HashMap<String, Ghost> ghosts) {
        super(account, controller, scoreboard, ghosts);
    }
}
